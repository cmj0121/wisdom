---
type: tool_used
tool: Skill
input_match: '"skill"\s*:\s*"agent-smith'
min: 1
---

Cutting settled work into units and running the iterations over them is the decomposition
and sequencing this skill owns. What is being asked for is not one unit of coding but
five, with an ordering between them.

Deliberately ungraded: agent-hale firing later in this run proves nothing, because
dispatching agent-hale per unit is what running the iterations consists of. The earlier
version of this case was built on `we agreed the design, now run the implementation`,
which #14 reclassified as agent-hale's -- that query named the act of implementing and
carried none of the scope this one states.
