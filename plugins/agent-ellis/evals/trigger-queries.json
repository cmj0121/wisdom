{
  "skill": "agent-ellis",
  "about": "Trigger eval queries for this skill's description. A query is labelled with whether the skill should activate for it; the negatives are near-misses that share vocabulary with the skill but need something else. Split fixed so successive description edits are compared on the same ground: tune against train, judge against validation. One validation positive was replaced by hand (issue #14): `does this change do what the ticket asked` is pr-flow's, not ellis's -- pr-flow's Phase 2 is `compare what the branch does with what the issue asked for`, its description says so, and its own train half already carries `does this PR actually cover everything issue #42 asked for?`. Ellis verifies acceptance against `PLAN.md`, not against a ticket, so the query was two sets racing over pr-flow's ground rather than a gap in this description.",
  "how_to_run": "`make eval` is the committed runner, but it covers only the queries a 3-sample blind-router baseline actually missed, not this whole file -- see CLAUDE.md for why 13 and not 380. To judge the rest: `skillgrade` runs a labelled set like this one and exits non-zero when the pass rate falls below a threshold. Needing nothing installed: run each query with `claude -p --output-format json` and count the runs whose transcript contains a Skill tool_use naming this skill. Cheapest of all, and closest to the router's real conditions: give a model only the name and description of every installed skill, with no bodies and no repo access, and have it pick one skill or none per query. `claude plugin eval`, which `make eval` drives, is generally available rather than early access; what it needs instead is a credential, a network and real money -- $0.142 a run, measured -- which is why it sits outside `make test` and CI does not run it. It will not reproduce this file query-for-query either: a bare query does not fire a skill, so a case is one of these queries rewritten as a realistic turn. This file stays the cheap, free, offline record.",
  "runs_per_query": 3,
  "trigger_rate_threshold": 0.5,
  "queries": [
    {
      "query": "review this change before it lands",
      "should_trigger": true,
      "split": "train"
    },
    {
      "query": "is this actually done, or does it just compile",
      "should_trigger": true,
      "split": "train"
    },
    {
      "query": "ellis, check the quality of this branch",
      "should_trigger": true,
      "split": "train"
    },
    {
      "query": "qa review this pr",
      "should_trigger": true,
      "split": "train"
    },
    {
      "query": "verify this meets the acceptance criteria",
      "should_trigger": true,
      "split": "train"
    },
    {
      "query": "go over this diff and tell me what's wrong with it",
      "should_trigger": true,
      "split": "train"
    },
    {
      "query": "run the tests and judge whether this is ready",
      "should_trigger": true,
      "split": "validation"
    },
    {
      "query": "check this for quality and security issues",
      "should_trigger": true,
      "split": "validation"
    },
    {
      "query": "is this branch clean enough to land -- quality, tests and acceptance",
      "should_trigger": true,
      "split": "validation"
    },
    {
      "query": "sanity-check my work before i merge",
      "should_trigger": true,
      "split": "validation"
    },
    {
      "query": "fix the bugs you find",
      "should_trigger": false,
      "split": "train"
    },
    {
      "query": "just run the tests",
      "should_trigger": false,
      "split": "train"
    },
    {
      "query": "do a full security audit of the project",
      "should_trigger": false,
      "split": "train"
    },
    {
      "query": "is this ready to run in production",
      "should_trigger": false,
      "split": "train"
    },
    {
      "query": "write tests for this",
      "should_trigger": false,
      "split": "train"
    },
    {
      "query": "plan the fixes for these findings",
      "should_trigger": false,
      "split": "train"
    },
    {
      "query": "explain why this test is failing",
      "should_trigger": false,
      "split": "validation"
    },
    {
      "query": "review the design, not the code",
      "should_trigger": false,
      "split": "validation"
    },
    {
      "query": "summarise this review shorter",
      "should_trigger": false,
      "split": "validation"
    },
    {
      "query": "open a pr for this branch",
      "should_trigger": false,
      "split": "validation"
    }
  ]
}
